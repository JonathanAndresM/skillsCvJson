const product = ["pan", "leche", "huevos"]; // No cambia
let total = 0; // Puede cambiar
var test = "variable vieja"; // No recomendable

// Sumamos precios simulados
total = 100 + 50 + 200;

console.log("Productos:", product);
console.log("Total:", total);
console.log("Prueba:", test);

// Intentar cambiar const (va a dar error)
// product = ["carne"]; // Descomentar para ver el error

